OP:CC-2 EXPANSION PROMPT — differential Galois group of L_2 (order 4)
Stage-2 block. Hand to the agent ONLY after op:cc-1 HALT review is approved.
======================================================================

ROLE
You are a research agent under SIARC four-class discipline (PROVEN / STRUCTURAL /
VERIFIED / CONJECTURED) with AEAL provenance. Same rules as op:cc-1: every
load-bearing claim gets exactly one class; PROVEN is Lean-only; numerics carry
script path + SHA-256 + dps; literature locators are flagged VERIFIED-by-citation
vs PROVEN-here; falsification-first. Maintain the EBR-II discipline line: a large
Galois group is EVIDENCE toward, not proof of, transcendence of the specific
connection entry C until a period/Liouvillian argument closes it.

CONTEXT — established by op:cc-1 (treat as VERIFIED inputs; do not re-derive)
  Object: EBR positive-b family, d = 2, b(n)=3n^2+n+1 (beta_2=3), g_n=Q_n/(2n)!,
  G(s)=sum g_n s^n. Operator L_2 = P0(theta) - s P1(theta+1) - s^2, theta=s d/ds,
  order 4 = 2d, D-form coefficients
     a_4=4s^4(4-3s), a_3=48s^3-94s^4, a_2=12s^2-156s^3, a_1=-30s^2, a_0=-s^2.
  Singular set {0, R=4/3, infinity}:
   * s=0  regular singular, exponents {0,1/2,1,3/2} (monodromy eigenvalues {1,-1,1,-1});
   * s=R  regular singular, exponents {0,1,2,-gamma}, gamma=11/6; -gamma resonates with
          {0,1,2} -> a LOG (non-semisimple local monodromy / Jordan block at eigenvalue 1);
   * s=oo IRREGULAR: single Newton slope 1/4, edge polynomial -12 c^4 - 1, ramification
          exactly 4 (one transitive cyclic order-4 orbit of determining factors
          ~ exp(C s^{1/4})); integer Poincare rank 1.
  L_2 is IRREDUCIBLE over C(s) and is the MINIMAL annihilator (op:cc-1 CC1-IRREDUCIBLE).
  Canonical hash of op:cc-1 inputs: bfb91bdeef251be00b770f486ec53d2304f4c1064f85d907a82951a49f5f227e
  C is the 0 -> R connection entry (exponent-0 Frobenius at 0 onto the dominant -gamma
  solution at R); transcendence of C is the open target (EBR-II Conj 6.1).

OBJECTIVE (op:cc-2)
Identify the differential Galois group G = Gal(L_2) over C(s) (an algebraic subgroup
of GL_4), or prove the strongest available constraint on it, and connect that
constraint to the (non-)elementarity of the connection coefficient C. Kovacic's
algorithm is order-2 only and does NOT apply at order 4; use the order-4 machinery
(van der Put-Singer "Galois Theory of Linear Differential Equations", ch. on
GL_2/GL_3/GL_4 and on the formal/local theory at irregular points; Compoint-Singer;
Hrushovski's algorithm as the decision-procedure backstop).

DECOMPOSE INTO OP-CODES (work in order; HALT + report at each gate)

op:cc-2a  LOCAL GALOIS DATA (cheap, mostly VERIFIED-able)
  - At s=oo build the local differential Galois group from the formal data: the
    exponential torus generated by the determining factors exp(C s^{1/4}) (single
    slope 1/4), the formal monodromy realizing the cyclic order-4 ramification, and
    the Stokes structure (one slope => Stokes directions counted by the slope). Give
    the local group's identity component (a 1-dimensional torus from the single
    exponential, twisted by the Z/4 ramification).
  - At s=0 and s=R give the local monodromy conjugacy classes: s=0 semisimple with
    eigenvalues {1,-1,1,-1}; s=R SEMISIMPLE pseudo-reflection {1,1,1,exp(pi i/3)}.
    [ERRATUM CC4-ERR-1: the original line read "s=R unipotent-times-eigenvalue (a
    Jordan block from the -gamma vs {0,1,2} resonance)". CORRECTED: M_R is SEMISIMPLE,
    there is NO logarithm at R. A -gamma resonance log requires gamma in Z; here
    gamma = 11/6 not in Z, so no Jordan block forms (cc2-2d, two channels; cc4-1
    no-log resonance residuals to 169 digits). Eigenvalues unchanged.]
  - Deliverable: the three local groups + the fact (from cc-1) that G acts
    IRREDUCIBLY on C^4. Grade VERIFIED for the formal/exponent computation (script+hash);
    STRUCTURAL for the local-group identification (cites the formal classification).

op:cc-2b  INVARIANT BILINEAR FORM (the decisive reductive test)
  - Compute the eigenring End(L_2) (commutant): irreducible (cc-1) => eigenring is a
    division algebra over C, expected = C (confirms absolute irreducibility). VERIFIED
    by computation if done; flag if it needs Sage.
  - Compute the symmetric square Sym^2 L_2 (order 10) and the exterior square Lambda^2
    L_2 (order 6) and test each for a rational solution (a horizontal vector = a
    G-invariant form on C^4):
      * invariant in Sym^2  => G preserves a symmetric form => G subset of O_4 (or SO_4);
      * invariant in Lambda^2 => G preserves a symplectic form => G subset of Sp_4;
      * neither => G is SL_4 or GL_4 (largest case, strongest for transcendence).
    FALSIFICATION TARGET here: "G is small / virtually solvable / Liouvillian." Run the
    order-4 Liouvillian-solution test (factor Sym/Lambda powers; search hyperexponential
    solutions of the symmetric powers). cc-1 already gives no order-1 factor of L_2 itself;
    extend to the symmetric powers. A clean null (no invariant + no Liouvillian solution)
    => G is one of the large reductive groups, the desired structural outcome.
  - Tooling reality: Sym^2 / Lambda^2 of an order-4 operator and their rational solutions
    are heavy; if pure-Python/sympy cannot, use Sage's ore_algebra (symmetric_power,
    rational_solutions) and RECORD it VERIFIED-by-tool with the Sage version + script hash;
    otherwise grade the bilinear-form classification STRUCTURAL with the explicit gap noted.

op:cc-2c  IDENTITY COMPONENT & DENSITY
  - From cc-2a (local generators: monodromies at 0, R + formal monodromy + exponential
    torus + Stokes matrices at oo) attempt to show these generate a Zariski-dense
    subgroup of the candidate group from cc-2b (Schlesinger/density: for the regular part
    the monodromies generate, and the irregular part adds the exponential torus + Stokes).
    Conclude G^0 (identity component) is reductive and as large as cc-2b allows.
  - Deliverable: G (or G^0) named, e.g. one of {SL_4, Sp_4, SO_4, SL_2 via Sym^3, ...},
    with the supporting constraint. Grade STRUCTURAL (CAS + hand, not Lean).

op:cc-2d  GALOIS => (NON-)ELEMENTARITY OF C
  - State precisely which group conclusion forces C non-Liouvillian / non-Gamma-quotient.
    Standard bridge: if G^0 is semisimple non-abelian (SL_2/Sp_4/SO_4/SL_4) and acts
    irreducibly, the Picard-Vessiot extension is NOT Liouvillian, so no solution (hence no
    connection entry built rationally from solutions) is Liouvillian; combined with the
    period interpretation (op:cc-3) this is the route to "C transcendental, not a
    Gamma-quotient." DO NOT assert transcendence from the group alone: the connection
    ENTRY is a specific period (a pairing of two specific solution germs), and one must
    still rule out that THIS entry lands in an elementary sub-orbit. Keep the final
    transcendence statement CONJECTURED; promote only the implication
    "G^0 large + irreducible => no Liouvillian solution => C not Liouvillian"
    if and when each link is established.
  - FALSIFICATION TARGET: "C is a Gamma-quotient after all." Re-run the op:cc-3 aggressive
    Gamma-monomial PSLQ scan to >=200 digits at d=2 against the new candidate basis implied
    by G (e.g. periods of the associated Sym/Lambda local system); document null with hash.

CONSTRAINTS
  - mpmath/Arb for numerics; dps stated; SHA-256 on every artifact; two independent channels
    where feasible (e.g. eigenring-from-L_2 vs invariant-from-Sym^2/Lambda^2).
  - Tool-gap honesty: if a step needs Sage ore_algebra / Maple DEtools[DFactor, symmetric_power],
    say so and grade STRUCTURAL or VERIFIED-by-tool accordingly; never silently upgrade.
  - The EBR-II discipline line is binding: non-rigidity (op:cc-1 P=d-1>0) and a large Galois
    group are EVIDENCE, not proof, for the transcendence of the specific entry C.

OUTPUT
  1. SIARC report: Position / per-op-code (2a-2d) / four-class grade table / numbered open
     problems with op-codes / AEAL claims_cc.jsonl appended (same schema as op:cc-1).
  2. Any Sage/CAS scripts pinned + version-stamped; #print-axioms only if anything reaches Lean.
  3. One-paragraph epistemic-status: what moved class (e.g. G constrained to Sp_4/SL_4) and
     what stays CONJECTURED (transcendence of C).

START
Begin with op:cc-2a (local Galois data) and op:cc-2b's eigenring + invariant-form test ONLY.
Produce the three local groups, confirm absolute irreducibility via the eigenring, and run
the Sym^2 / Lambda^2 invariant-form test (refuting or finding a preserved form). HALT for
review before op:cc-2c. Report under the four-class discipline.
